# Backend API URL (without /api suffix - it's added in the code)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Pusher Configuration (Get from https://pusher.com - must match backend)
NEXT_PUBLIC_PUSHER_APP_KEY=your-app-key
NEXT_PUBLIC_PUSHER_APP_CLUSTER=mt1
